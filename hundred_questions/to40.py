# 31問目
# month = {1: "睦月", 2: "如月", 3: "弥生"}
# x = month.pop(3)  # キー2の要素を削除して取得
# print(f"new dict {month}, removed element: {x}")

# 32問目
# month = {1: "睦月", 2: "如月", 3: "弥生"}
# keys = list(month.keys())
# vaules = list(month.values())
# print(f"This list has keys: {keys}, values: {vaules}")

# 33問目
# numbers = {1,2,3,4,5} # 集合の定義
# print(numbers)

# 34問目
# numbers = {1, 2, 3, 4, 5}
# numbers.add(6)  # 集合に要素を追加
# numbers.remove(1)
# print(numbers)

# 35問目
# numbers = [1, 1, 5, 2, 5, 3, 3]
# # リストを集合に変換して重複を削除し、新しいリストを作成
# unique_numbers = list(set(numbers))
# print(unique_numbers)

# 36問目
# group_a = {1, 2, 3, 4, 5}
# group_b = {4, 5, 6, 7, 8}
# intersection = group_a & group_b  # 積集合
# print(intersection)

# 37問目
# group_a = {1, 2, 3, 4, 5}
# group_b = {4, 5, 6, 7, 8}
# difference = group_a - group_b  # 差集合
# print(difference)

# 38問目
# group_a = {1, 2, 3, 4, 5}
# group_b = {4, 5, 6, 7, 8}
# plus = group_a | group_b  # 和集合
# print(plus)

# 39問目
# numbers = {1,12,5,10,13,7,90}
# sorted_numbers = sorted(numbers)  # 集合をソートしてリストに変換
# print(sorted_numbers[-2])

# 40問目 タプルの作成
# screen_size = (1920, 1080)  # 解像度を表すタプル
# print(screen_size)
